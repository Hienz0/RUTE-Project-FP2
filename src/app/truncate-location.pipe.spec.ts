import { TruncateLocationPipe } from './truncate-location.pipe';

describe('TruncateLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
